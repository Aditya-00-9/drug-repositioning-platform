# OpenAI (required for AI-powered drug/disease profiles)
# Copy to .env.local and set your key. Restart the dev server after changing.
# On Vercel, set OPENAI_API_KEY in Project Settings → Environment Variables.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: model for chat (default used by /api/chat)
# OPENAI_CHAT_MODEL=gpt-4o-mini
